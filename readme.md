**Installation**

    # add ./bin to your path
    git clone git://github.com/allonhadaya/git-utils.git && cd git-utils && ./install && . ~/.bashrc

**Usage**

    pullall [dir]
    statall [dir]
    pruneall [dir]
    branchall [dir]
