**Installation**

    git clone git://github.com/allonhadaya/git-utils.git /opt/git-utils && cd git-utils && ./install && . ~/.bashrc

**Usage**

    pullall [dir]
    statall [dir]
    pruneall [dir]
    branchall [dir]
